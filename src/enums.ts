// Define and export an enum for user permissions
export enum Permissions {
  ADMIN = "ADMIN", // Admin permission with the value "ADMIN"
  READ_ONLY = "READ_ONLY", // Read-only permission with the value "READ_ONLY"
}

// Define and export an enum for loyalty user types
export enum LoyaltyUser {
  GOLD_USER = "GOLD_USER", // Gold user with the value "GOLD_USER"
  SILVER_USER = "SILVER_USER", // Silver user with the value "SILVER_USER"
  BRONZE_USER = "BRONZE_USER",  // Bronze user with the value "BRONZE_USER"
}
