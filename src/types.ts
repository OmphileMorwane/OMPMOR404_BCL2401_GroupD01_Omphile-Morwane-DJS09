// Define and export a type for Price, which can only take values 25, 30, 35, or 45
export type Price = 25 | 30 | 35 | 45
// Define and export a type for Country, which can only take specific country names
export type Country = 'Colombia' | 'Poland' | 'United Kingdom' | 'Malaysia'
